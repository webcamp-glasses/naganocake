<%= form_with model: @genre,  local:true do |f| %>

  <div class="form-group">
    <%= f.label :ジャンル %>
    <%= f.text_field :genre %>
  </div>

  <div class="form-group">
    <%= f.submit "変更を保存", class: "btn btn-success" %>
  </div>
<% end %>