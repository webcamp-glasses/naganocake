<h2>マイページ</h2>

<p>
  <strong>登録情報</strong>
  <%= link_to "編集する", edit_end_users_path, class: "btn btn-success" %>
</p>

<table class="table table-bordered" >
  <tbody>
    <tr>
      <td>氏名</td>
      <td><%= last_name(@end_user) %> <%= first_name(@end_user) %></td>
    </tr>
    <tr>
      <td>カナ</td>
      <td><%= last_kana_name(@end_user) %> <%= first_kana_name(@end_user) %></td>
    </tr>
    <tr>
      <td>郵便番号</td>
      <td><%= @end_user.postal_code %></td>
    </tr>
    <tr>
      <td>住所</td>
      <td><%= @end_user.address %></td>
    </tr>
    <tr>
      <td>電話番号</td>
      <td><%= @end_user.phone_number %></td>
    </tr>
    <tr>
      <td>メールアドレス</td>
      <td><%= @end_user.email %></td>
    </tr>
  </tbody>
</table>

<p>
  <strong>配送先</strong>
  <%= link_to " 一覧を見る", addresses_path, class: "btn btn-success" %>
</p>
<p>
  <strong>注文履歴</strong>
  <%= link_to " 一覧を見る", orders_path, class: "btn btn-success" %>
</p>